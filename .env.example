# Configurações do Servidor
PORT=3000
NODE_ENV=development

# Banco de Dados (PostgreSQL)
# Exemplo local: postgres://user:password@localhost:5432/radial_training
# Em produção (Railway/Render) a variável DATABASE_URL é fornecida automaticamente
DATABASE_URL=

# SMTP (opcional, para envio de certificados por e-mail)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Produção (Render/Railway definem porta automaticamente)
# NODE_ENV=production
