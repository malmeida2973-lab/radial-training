# ===============================================
# Radial Training - Variáveis de Ambiente
# ===============================================

# CONFIGURAÇÃO DO SERVIDOR
PORT=3000
NODE_ENV=production

# BANCO DE DADOS (PostgreSQL via Railway)
# Fornecido automaticamente pelo Railway
# Exemplo local: postgres://user:password@localhost:5432/radial_training
DATABASE_URL=postgresql://user:password@host:5432/database

# CONFIGURAÇÃO DE EMAIL
# Escolha: API da Brevo (recomendada) ou SMTP
# Provider API (Brevo)
EMAIL_PROVIDER=brevo
EMAIL_API_KEY=sua_chave_brevo
EMAIL_FROM="Radial Treinamentos <nao-responda@seu-dominio.com>"

# Fallback SMTP (caso queira usar SMTP em outro host)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seu_email@gmail.com
SMTP_PASS=sua_senha_app_ou_smtp
SMTP_FROM="Radial Treinamentos <seu_email@gmail.com>"

# URL BASE DA APLICAÇÃO
# Usado nos links dos emails
APP_URL=https://radial-training-production.up.railway.app

# ===============================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# ===============================================

# Para Gmail:
# 1. Ative autenticação em duas etapas em https://myaccount.google.com
# 2. Gere uma "Senha de app" em https://myaccount.google.com/apppasswords
# 3. Copie a senha de 16 caracteres para SMTP_PASS
# 4. Use o email do Gmail para SMTP_USER

# Para outros provedores de email:
# - Outlook: smtp-mail.outlook.com (porta 587)
# - Yahoo: smtp.mail.yahoo.com (porta 587)
# - Servidor próprio: seu_smtp.com (porta dependente)

# No Railway:
# 1. Abra seu projeto no Railway
# 2. Vá em "Variables"
# 3. Adicione cada variável com seu valor
# 4. Clique em "Deploy" para aplicar as mudanças

